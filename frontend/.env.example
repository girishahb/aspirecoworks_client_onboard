# Backend API base URL (no trailing slash). Used for all API requests.
# Unset: production default https://api.aspirecoworks.in
# Local dev: set to http://localhost:3000
# VITE_API_URL=http://localhost:3000
